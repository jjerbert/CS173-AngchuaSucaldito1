using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class xSign_Control : MonoBehaviour
{
    // Start is called before the first frame update
    public float floatingTime = 0;
    void Start()
    {
    }

    // Update is called once per frame
    void Update()
    {
        /*
        floatingTime += Time.deltaTime;
        if (GameFlow.xSign == "y")
        {
            Instantiate(gameObject, new Vector3(2.14374924f,2.88863134f,-3.0095048f), new Quaternion(0f,0.707106829f,0f,0.707106829f));
        }

        if (floatingTime > 3)
        {
            GameFlow.xSign = "n";
            Destroy(gameObject);
        }*/
    }
}
