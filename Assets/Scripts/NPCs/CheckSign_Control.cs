using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CheckSign_Control : MonoBehaviour
{
    public float floatingTime = 0;
    void Start()
    {
    }

    // Update is called once per frame
    void Update()
    {
        /*
        floatingTime += Time.deltaTime;
        if (GameFlow.checkSign == "y")
        {
            Instantiate(gameObject, new Vector3(2.14199996f,2.88863134f,-2.99000001f), new Quaternion(0f,0.707106829f,0f,0.707106829f));
        }

        if (floatingTime > 3)
        {
            GameFlow.checkSign = "n";
            Destroy(gameObject);
        }*/
    }
}
